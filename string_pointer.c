#include<stdio.h>

int main()
{

    char *canChange = "Hello World";
    puts(canChange);
    canChange = "Sagar";
    puts(canChange);



    char cannotChange[] ="hello world";
    puts(cannotChange);

    //cannotChange = "hiii";
    //puts("cannotChange");

    //gets(cannotChange);











    return 0;
}